{
  "name": "nostra-culqi-functions",
  "version": "1.1.0",
  "private": true,
  "description": "Backend seguro de Grupo Nostradamus para pagos, conciliación y control de asistencia.",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "check": "node --check index.js && node --check backend.js && node --check webhook.js && node --check asistencia.js && node --check lib/common.js && node --check lib/pricing.js && node --check lib/culqi3ds.js && node --check lib/culqi-webhook.js && node --check scripts/nostrachat-retention-backfill.js && node --test test/*.test.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.4.0"
  }
}