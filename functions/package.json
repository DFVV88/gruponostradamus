{
  "name": "nostra-culqi-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Backend seguro de Grupo Nostradamus para validar tarifarios y procesar pagos con Culqi.",
  "main": "backend.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "check": "node --check backend.js && node --check lib/common.js && node --check lib/pricing.js && node --check lib/culqi3ds.js && node --test test/*.test.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.4.0"
  }
}
